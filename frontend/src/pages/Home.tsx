import Chat from "@/components/Chat";

const Home = () => {
  return (
    <div className="flex flex-col gap-4 items-center justify-center h-[calc(100vh-7rem)]">
      <Chat />
    </div>
  );
};

export default Home;
