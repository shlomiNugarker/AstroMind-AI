import Chat from "@/components/Chat";

const Home = () => {
  return (
    <div className="flex flex-col gap-4 items-center justify-center py-3 h-[calc(100vh-76px)]">
      <Chat />
    </div>
  );
};

export default Home;
