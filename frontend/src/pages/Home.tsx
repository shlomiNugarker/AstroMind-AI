import Chat from "@/components/Chat";

const Home = () => {
  return (
    <div className="flex items-center justify-center h-[calc(100vh-80px)]">
      <Chat />
    </div>
  );
};

export default Home;
